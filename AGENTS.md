# 仓库目标

本仓库实现扶摇同花顺金融数据 REST API 的 Rust 客户端 crate。上游文档描述目标契约，
仓库代码描述当前已实现能力；不得因上游存在某个端点而宣称 crate 已支持它。

## 开始工作

1. 涉及标的、市场、日期、复权、行情、财务或基金语义时，先读取
   [`CONTEXT.md`](./CONTEXT.md)，并沿用其中的规范术语。
2. 检查 `Cargo.toml`、`src/`、测试和 `mise.toml`，确认当前公共 API、模块惯例、依赖与
   可执行质量门。
3. 以具体端点页面为契约依据；先用
   [API 索引](https://fuyao.aicubes.cn/llms.txt)定位端点，只在需要完整参数、字段或错误语义时
   读取[完整聚合文档](https://fuyao.aicubes.cn/llms-full.txt)。通用约定以
   [API 总览](https://fuyao.aicubes.cn/docs/api-reference/overview/)为准。
4. 对照请求、上游契约与现有实现列出本次差异。标为“敬请期待”的能力不进入实现范围。

## 设计边界

- 复用优先级依次为仓库既有抽象、Rust 标准能力、成熟且持续维护的生态 crate；实施前记录
  候选、证据、淘汰理由与最小自研范围。
- 由一个深模块统一拥有 Base URL、`X-api-key` 注入、HTTP 传输、响应信封解码和公共错误
  映射；端点模块只拥有各自的请求参数与业务数据映射。
- 将网络、解码和非零业务结果建模为可区分的失败。HTTP 200 只表示传输成功，不能替代
  响应信封中业务结果的判断。
- 用经过验证的领域类型表达完整标的代码、日期区间和有限枚举。外部字符串只在边界解析，
  不用裸字符串、布尔组合或哨兵值扩散非法状态。
- 保留上游的缺失语义、顺序、游标和单位。`null` 不补零，不透明游标原样回传，不把自然日
  当作交易日，也不在端点契约之外擅自规范化参数。
- API Key 只从调用方配置或环境注入；日志、错误、快照、fixture 和示例均使用脱敏占位值。
- 公共 API 的命名使用 Rust 惯例，协议字段使用准确的序列化映射；不得为追求字段同名而牺牲
  Rust 接口清晰度。
- `CONTEXT.md` 只维护稳定领域词汇。HTTP、模块、依赖、序列化和错误类型等实现决定留在代码
  或满足 ADR 门槛后记录于 ADR。

## 开发闭环

1. 工具版本和任务入口统一放在根 `mise.toml`；文件缺失时，首次代码任务先建立满足仓库约束
   的最小配置。所有项目任务通过 `mise run <task>` 执行。
2. 按 Red-Green-Refactor 实施每个端点：先用最小请求/响应 fixture 建立失败契约，再完成映射，
   最后在测试持续通过时重构。
3. 默认测试使用本地 mock transport 或 mock server，不依赖网络、真实账号、实时数据或 API
   Key。实时契约测试必须显式启用并默认忽略。
4. 每个端点至少覆盖成功结果、非零业务结果、缺失可选数据和边界参数；共享层还需覆盖传输
   失败、无效响应、认证失败、权限不足与频率超限。
5. 完成前运行仓库 `mise.toml` 定义的格式、静态检查、测试和文档任务。没有对应任务时先补齐
   任务定义，再通过 `mise run` 执行。
6. 对照实际 diff 复查上游契约、公共 API、凭证处理和测试覆盖。交付时列出已支持的端点、
   执行证据、未运行项和上游仍未保证的残余风险。
